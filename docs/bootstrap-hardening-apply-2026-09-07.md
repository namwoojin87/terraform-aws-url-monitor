# Bootstrap 보완 실제 적용 결과 — 2026-09-07

## 결과

사용자가 검토한 **키·별칭 추가 2개, 배포 정책·S3 정리 규칙 수정 2개, 삭제 0개**의 저장 계획을 승인했고, 서울 리전의 기존 비루트 운영자 세션에서 그 계획을 한 번 적용했다.

- 적용 시작: 18:01:38 KST / 09:01:38 UTC.
- 적용 완료: 18:02:41 KST / 09:02:41 UTC, Terraform 종료 코드 0.
- 직접 조회 및 최종 검증: **18:04:34 KST / 09:04:34 UTC**.
- 적용 결과: `2 added, 2 changed, 0 destroyed`.
- 적용 후 전체 bootstrap 계획: **No changes**, 종료 코드 0.
- 원격 bootstrap 상태: 관리 리소스 **16 → 18개**, 상태 serial **4 → 6**.
- Scheduler 전체 설정·입력: 변경 없이 **DISABLED**.

## 확인한 항목

| 대상 | 실제 확인 결과 |
| --- | --- |
| KMS 키·별칭 | 고객 관리 대칭 키가 Enabled, 단일 리전, ENCRYPT_DECRYPT. 별칭·상태·출력·배포 정책이 같은 키를 가리킴 |
| 키 정책·회전 | 승인한 전체 키 정책과 일치. 자동 회전 활성화, 주기 365일. 태그 일치 |
| 배포 정책 | 새 기본 버전의 전체 14개 문장이 승인한 계획과 의미상 일치. 생성된 정확한 키 ARN으로 DescribeKey 제한 |
| 정책 이력 | 버전 3개에서 4개로 증가. 기존 버전 삭제 없음 |
| 상태 저장소 | 미완료 멀티파트 업로드 중단 7일 추가. 기존 과거 버전 90일 만료 및 나머지 설정 유지 |
| 신뢰·접근 경계 | 두 GitHub 역할의 식별 정보·신뢰·정책 연결·인라인 정책 목록 불변. 배포 정책 연결 대상 및 권한 경계 사용 불변. S3 bootstrap 접근 차단 정책 불변 |

새 배포 정책 문장 4개는 현재·이력 테이블 2개의 PITR 설정, `url-monitor-checker` 1개의 비동기 호출 설정, Scheduler/Lambda 실패 큐 2개의 수명주기 관리, 새 알림 키 1개의 `DescribeKey`로 제한했다. 기존 프로젝트 경보 조회는 유지하고 와일드카드 리소스의 `cloudwatch:DescribeAlarms`만 제거했다. 큐 메시지 수신·삭제·비우기 권한이나 GitHub의 직접 키 관리 권한은 추가하지 않았다.

Terraform의 `prevent_destroy`와 삭제 대기 30일은 검토한 소스·계획의 보호 설정이다. Enabled 상태의 KMS 조회만으로 AWS 콘솔/API의 키 삭제를 막는 보호 장치를 확인했다고 주장하지 않는다. 키 정책의 계정 IAM 위임은 승인 범위에 포함됐으며, 계정 관리자가 IAM으로 키 사용·관리 권한을 부여할 수 있는 신뢰 경계가 남는다.

## 검증 방법과 증빙 경계

적용 직전에 원래 저장 계획의 SHA-256, 소스와 잠금 파일, 운영자 및 원격 상태를 확인했다. 새로 조회한 전체 계획의 리소스 변경·출력 변경이 승인된 계획과 동일함을 비교한 후 **원래 승인된 저장 계획**을 적용했다. 적용 후 AWS에서 실제 키 메타데이터·정책·회전, IAM의 새 기본 정책, S3 규칙과 Scheduler를 다시 읽었다. API 응답 메타데이터와 역할의 사용 시각 정보는 설정 불변 비교에서 제외했다.

- 기준 커밋: `c9241002f80b17d399455c4a73c77c291741ba95`.
- 추가 소스 변경: 공식 HashiCorp 서명으로 검증한 bootstrap Linux 공급자 해시 1줄. AWS 공급자 버전은 6.62.0 그대로.
- 잠금 파일 Git blob: `75423324b99a3ba8d770a8b356e72792cb653bea`.
- 적용한 계획 SHA-256: `e96c76fac0f8b299e22b029e2b4e167765f1e5f126c5884b941871aa1f98b542`.

원본 계획·입력·상태·계정 식별자·정책 스냅샷·실행 로그는 비공개 CloudShell 작업 디렉터리에 보관했다. 이 기록에는 자격증명이나 개인정보를 포함하지 않는다. 적용한 저장 계획은 재사용하지 않는다.

## 게시 전 로컬 재검증 — 09:25–09:26 UTC

2026-09-07 09:25:23–09:26:39 UTC에 위 기준 커밋과 Linux 잠금 파일 수정, 적용 상태 문서 변경을 포함한 작업 트리를 다시 검사했다. Terraform 1.16.0의 형식 검사·세 루트 validate, bootstrap mock **9개**와 module mock **11개**, TFLint 0.64.0 및 공백 검사는 통과했다. Python 3.12 전체 테스트는 **71 passed, 0 skipped**였다.

Checkov 3.3.16 전체 검사는 **37 resources / 195 passed / 16 failed / 0 skipped / 0 parsing errors**, scanner와 gate 종료 코드 **1**이었다. [잔여 지적 목록](security-review.md#proposed-exceptions-for-owner-review)은 이전 코드 검증과 같고 예외는 추가하지 않았다. 기능 테스트 통과와 보안 gate 실패는 별개의 결과다. 이 로컬 재검증은 AWS 호출·apply·알림 시험 없이 진행했으며, 아래 운영 미배포 상태를 바꾸지 않는다. 이 절을 포함한 후속 문구 보완은 문서만 변경한다.

## 아직 하지 않은 작업

이번 결과는 **bootstrap 기반 적용 완료**이지 전체 프로젝트의 신규 운영 구성 배포 완료가 아니다. 대시보드, 두 실패 큐, PITR, Lambda 비동기 설정·추적, SNS 토픽 암호화·런타임 발행자 권한은 별도 운영 배포가 남아 있다. 알림 발송·Lambda 호출·장애 시연은 수행하지 않았다.

보안 검사 16건에 대한 일괄 예외, 검사 우회, main 병합, GitHub 운영 배포, 자동 검사 활성화는 하지 않았다. bootstrap의 변경 없음 결과가 runtime 상태나 이메일 수신 성공을 검증하는 것은 아니다. 새 키의 저장 요금은 자동 검사 OFF와 별개로 유지된다. 비용·회전·삭제 검토는 [SNS 키 운영 절차](sns-encryption-runbook.md#cost-and-lifecycle)를 따른다.

이 기록과 Linux 잠금 파일 수정은 draft PR #8의 후속 검토 대상이며, 기록 반영 자체가 main 병합이나 운영 배포를 승인하지 않는다. 후속 작업은 소스 통합 및 잔여 보안 판단을 거친 새 runtime 저장 계획 검토와 production 승인이다. 이전 main의 bootstrap 코드나 과거 저장 계획을 그대로 적용하지 않는다.
